sed -i 's/val currentLanguage = _currentLanguage.asStateFlow()/val currentLanguage = _currentLanguage.asStateFlow()\n\n    private val _streak = kotlinx.coroutines.flow.MutableStateFlow(0)\n    val streak = _streak.asStateFlow()\n\n    fun setStreak(s: Int) {\n        _streak.value = s\n    }\n\n    private val _isDarkMode = kotlinx.coroutines.flow.MutableStateFlow(false)\n    val isDarkMode = _isDarkMode.asStateFlow()\n\n    fun setDarkMode(dark: Boolean) {\n        _isDarkMode.value = dark\n    }/' app/src/main/java/com/example/ui/TinyPawsViewModel.kt
